spring.application.name=reservation-management-service

# Server Configuration
server.port=8083

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/reservation_service?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=HOB1campus321
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Email Configuration (Gmail SMTP)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=your-email@gmail.com
spring.mail.password=your-app-password
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Email Template Configuration
reservation.email.from=noreply@colombobookfair.lk
reservation.email.subject=Colombo BookFair - Reservation Confirmation

# JWT Configuration (for token validation)
jwt.secret=4bb6d1dfbafb64a681139d1586b6f1160d18159afd57c8c79136d7490630407c
jwt.expiration=86400000

# File Upload Configuration (for QR codes)
file.upload.dir=./uploads/qrcodes

# Business Logic Configuration
reservation.max-stalls-per-user=3
reservation.event.start-date=2026-03-15
reservation.event.end-date=2026-03-21

# External Service URLs
service.auth.url=http://localhost:9090/api/auth
service.stall.url=http://localhost:8082/api/stalls
service.profile.url=http://localhost:8081/api/profiles

# CORS Configuration
cors.allowed-origins=http://localhost:3000,http://localhost:5173
